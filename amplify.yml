version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd webapp/frontend
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: webapp/frontend/dist
    files:
      - '**/*'
  cache:
    paths:
      - webapp/frontend/node_modules/**/*
